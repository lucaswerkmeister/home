# write metadata to video file’s extended attributes
--xattrs
# add metadata to video file itself
--add-metadata
# download subtitles
--write-sub
--embed-subs
# all except YouTube live chat, don’t care for that
--sub-langs all,-live_chat
# don’t set file modification time based on Last-Modified header
--no-mtime
