AddKeysToAgent yes
SendEnv LANG LC_*

Include config.d/*

# only set these if not already set in config.d
ControlMaster auto
ControlPath /run/user/%i/ssh-controlmasters/%C
ControlPersist 10m
IdentitiesOnly yes
